{
  "_manual": [
    "One file = one sequence. Load can select multiple JSON files. See sequence_authoring.md for the full guide.",
    "Required: FormatVersion=1, Name=nonblank string, RepeatCount=integer 1..9999, Steps=array (0..10000).",
    "Each step requires nonblank CommandText, LineEndingMode=null/None/Cr/Lf/Crlf, DelayAfterMs=integer 0..600000. Name and Comment are optional string/null.",
    "No unknown fields, duplicate keys, comments or trailing commas. Any invalid file or duplicate sequence name cancels the entire batch.",
    "Run uses the main Terminal/HEX mode. Delays are after sending. No response wait or branching. Commands here are examples; replace them with your device protocol.",
    "Load limits: 100 files, 2 MiB per file, 10000 total steps and 16 MiB total input per batch. Exceeding a limit cancels the entire batch."
  ],
  "FormatVersion": 1,
  "Name": "상태 확인 3회",
  "RepeatCount": 3,
  "Steps": [
    {
      "Name": "상태 조회",
      "CommandText": "status",
      "LineEndingMode": "Crlf",
      "DelayAfterMs": 500,
      "Comment": "전송 후 500ms 대기. 응답 성공 여부는 검사하지 않음."
    },
    {
      "Name": "버전 조회",
      "CommandText": "version",
      "LineEndingMode": "Crlf",
      "DelayAfterMs": 1000,
      "Comment": null
    }
  ]
}
